Copyright 2026 The Trivalent Authors

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software distributed under the License is
distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and limitations under the License.
---
diff --git a/extensions/common/manifest_handlers/permissions_parser.cc b/extensions/common/manifest_handlers/permissions_parser.cc
index e4bb829059..72b45f2a0f 100644
--- a/extensions/common/manifest_handlers/permissions_parser.cc
+++ b/extensions/common/manifest_handlers/permissions_parser.cc
@@ -107,7 +107,8 @@ bool ParseHostsFromJSON(Extension* extension,
   const base::Value::List& list = permissions->GetList();
   for (size_t i = 0; i < list.size(); ++i) {
     if (list[i].is_string()) {
-      hosts->push_back(list[i].GetString());
+      if (list[i].GetString() != URLPattern::kAllUrlsPattern)
+        hosts->push_back(list[i].GetString());
     } else {
       *error = ErrorUtils::FormatErrorMessageUTF16(
           errors::kInvalidHostPermission, key, base::NumberToString(i));
@@ -427,8 +428,8 @@ bool PermissionsParser::Parse(Extension* extension, std::u16string* error) {
   if (extension->manifest_version() >= 3) {
     std::vector<std::string> manifest_hosts;
     std::vector<std::string> manifest_optional_hosts;
-    if (!ParseHostsFromJSON(extension, keys::kHostPermissions, &manifest_hosts,
-                            error)) {
+    if (!ParseHostsFromJSON(extension, keys::kHostPermissions,
+                            &manifest_optional_hosts, error)) {
       return false;
     }
 
@@ -438,10 +439,6 @@ bool PermissionsParser::Parse(Extension* extension, std::u16string* error) {
     }
 
     // TODO(kelvinjiang): Remove the dependency for |api_permissions| here.
-    ParseHostPermissions(extension, keys::kHostPermissions, manifest_hosts,
-                         initial_required_permissions_->api_permissions,
-                         &initial_required_permissions_->host_permissions);
-
     ParseHostPermissions(extension, keys::kOptionalHostPermissions,
                          manifest_optional_hosts,
                          initial_optional_permissions_->api_permissions,
diff --git a/extensions/common/manifest_handlers/web_accessible_resources_info.cc b/extensions/common/manifest_handlers/web_accessible_resources_info.cc
index 719d252a3d..a624443091 100644
--- a/extensions/common/manifest_handlers/web_accessible_resources_info.cc
+++ b/extensions/common/manifest_handlers/web_accessible_resources_info.cc
@@ -294,17 +294,17 @@ bool WebAccessibleResourcesInfo::HasWebAccessibleResources(
 // static
 bool WebAccessibleResourcesInfo::ShouldUseDynamicUrl(const Extension* extension,
                                                      const std::string& path) {
+  bool ret = true;
   const WebAccessibleResourcesInfo* info = GetResourcesInfo(extension);
   if (!info) {
-    return false;
+    return ret;
   }
   for (const auto& entry : info->web_accessible_resources) {
-    if (extension->ResourceMatches(entry.resources, path) &&
-        entry.use_dynamic_url) {
-      return true;
+    if (extension->ResourceMatches(entry.resources, path)) {
+      ret = entry.use_dynamic_url;
     }
   }
-  return false;
+  return ret;
 }

 WebAccessibleResourcesInfo::Entry::Entry() = default;
