#!/bin/bash

# Copyright 2025 The Trivalent Authors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software distributed under the License is
# distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and limitations under the License.

ARCH="$(uname -m)"
CHROMIUM_FLAGS=""
FEATURES=""

# USE_WAYLAND=[true|false|unknown]
if command -v nvidia-smi && [ -n "$DISPLAY" ]; then
	echo "Nvidia + X11 detected"

	# Nvidia may or may not use Wayland when Xwayland is available
	# We don't want to do anything specific to either Wayland or X11 in this case
	USE_WAYLAND="unknown"
elif [ "$USE_WAYLAND" == "true" ]; then
    CHROMIUM_FLAGS+=" --ozone-platform=wayland"
elif [ -z "$USE_WAYLAND" ]; then
	case "$XDG_SESSION_TYPE" in
	   wayland)
	      USE_WAYLAND="true"
	      ;;
	   x11)
	      USE_WAYLAND="false"
	      ;;
	   *)
	      USE_WAYLAND="unknown"
	      ;;
	esac
fi
[ "$USE_WAYLAND" == "false" ] && CHROMIUM_FLAGS+=" --ozone-platform=x11"

# ENABLE_VULKAN=[true|false]
ENABLE_VULKAN="false"

# Other architectures are not tested for and should not be included yet
if [ "$ARCH" == "x86_64" ] ; then
	if [ "$USE_WAYLAND" == "false" ]; then
		CHROMIUM_FLAGS+=" --enable-gpu-memory-buffer-video-frames"
		CHROMIUM_FLAGS+=" --enable-zero-copy"
		CHROMIUM_FLAGS+=" --ignore-gpu-blocklist"
		CHROMIUM_FLAGS+=" --disable-gpu-driver-bug-workaround"

		# Enable Vulkan
		# (Vulkan is not supported on Wayland, it is also experimental)
		ENABLE_VULKAN="true"
	fi

	if [ "$ENABLE_VULKAN" == "true" ]; then
		CHROMIUM_FLAGS+=" --use-angle=vulkan --use-vulkan"
		FEATURES+="Vulkan,DefaultANGLEVulkan,VulkanFromANGLE,VaapiIgnoreDriverChecks"
	fi
fi

[ -n "$FEATURES" ] && CHROMIUM_FLAGS+=" --enable-features=$FEATURES"

if [ "$HARDEN_BROWSER_LAUNCH" == "true" ]; then
	# This improves security by disabling risky features and enabling stronger sandboxing
	# This also reduces fingerprinting ability significantly
	CHROMIUM_FLAGS+=" --js-flags=--jitless" # Disable JIT
	CHROMIUM_FLAGS+=" --enable-strict-powerful-feature-restrictions" # Disable features with low security
	CHROMIUM_FLAGS+=" --disable-extensions" # Disable extensions
	CHROMIUM_FLAGS+=" --disable-remote-fonts" # Disable remote font rendering
	CHROMIUM_FLAGS+=" --disable-reading-from-canvas" # Block rendering Canvas API
	CHROMIUM_FLAGS+=" --disable-3d-apis" # Disable WebGL
	CHROMIUM_FLAGS+=" --disable-gpu" # Disable hardware acceleration
	CHROMIUM_FLAGS+=" --gpu-sandbox-start-early --gpu-sandbox-failures-fatal" # Force enable the GPU sandbox
	CHROMIUM_FLAGS+=" --use-fake-device-for-media-stream" # Disable audioand camera input using fake devices
fi
